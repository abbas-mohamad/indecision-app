export const isAdult = x => x >= 18;

export const canDrink = x => x >= 18;

export default age => age >= 64;

// const isSenior = age => age >= 64;

// export {isAdult, canDrink, isSenior as default}